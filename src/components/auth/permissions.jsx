/* eslint-disable react-refresh/only-export-components */
// Compatibility wrapper for the canonical permissions module.
// The real implementation lives in permissions.js so there is a single source of truth.
export * from './permissions';
export { default } from './permissions';